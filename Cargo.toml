[package]
name = "example_3d_next"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ultraviolet = {version = "0.9.2", features = ["int"] }
gamercade_rs = "0.1.1"

[lib]
crate-type = ['cdylib']

[profile.release]
lto = true